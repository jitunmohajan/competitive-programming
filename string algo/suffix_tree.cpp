#include<bits/stdc++.h>
using namespace std;

struct node{
ed[27]={0};
};

vector<node>adj;

int arr(string a,int len){
adj.push_back(node());
for(int i=0;i<len;i++)
   if(adj.ed[a[i]-65]==0)
       adj.ed[a[i]-65]=1;

}

int main(){

int n;
cin>>n;
string a;


for(int i=0;i<n;i++)
{
    cin>>a;
    int len=a.length();
    arr(a,len);                                                                                                        )
}


}
